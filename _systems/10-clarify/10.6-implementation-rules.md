---
route: "설계 완료 → 구현 시작 전 준수사항"
format: "코딩 규칙 + 호환성 체크 + TDD 프로세스"
context: "Python 3.11.7 + Git Bash 환경에서 일관성 있는 구현. 모든 개발자가 동일 표준 준수."
rules: "모든 코드는 TDD 원칙 준수. 테스트 먼저, 구현 나중. 호환성 체크 필수."
---

# 10.6. 구현 규칙

## 환경 요구사항

**필수 버전:**
\`\`\`
Python: 3.11.7 (정확히)
Git Bash: 최신 버전
OS: Windows / Linux / macOS
\`\`\`

**Python 버전 확인:**
\`\`\`bash
python --version
# Python 3.11.7 출력 필수
\`\`\`

---

## 코딩 규칙

### 1️⃣ Python 3.11.7 호환성

**필수 항목:**
\`\`\`python
# ✅ 필수: Type hints
def process_file(filename: str) -> dict:
    pass

# ✅ 필수: f-strings
name = "Alice"
message = f"Hello, {name}"

# ✅ 필수: dataclasses (3.7+)
from dataclasses import dataclass

@dataclass
class Config:
    timeout: int = 30
    retry: int = 3
\`\`\`

---

### 2️⃣ Git Bash 호환성

**경로 작성:**
\`\`\`python
# ❌ 금지: Windows 경로 직접 작성
path = "C:\\Users\\user\\file.txt"

# ✅ 허용: pathlib 사용
from pathlib import Path
path = Path("C:/Users/user/file.txt")
# 또는
path = Path.home() / "file.txt"
\`\`\`

---

## TDD 프로세스

### 테스트 작성 규칙

**경로:** \`__test__/{YYYYMMDD}_{title}/test_{module}.py\`

**테스트 파일 구조:**
\`\`\`python
import unittest
from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).parent.parent.parent / "_scripts"))

class TestMain(unittest.TestCase):
    def test_function_returns_expected_output(self):
        """테스트: 함수가 예상 출력을 반환"""
        result = some_function({"key": "value"})
        self.assertEqual(result["status"], "success")

if __name__ == "__main__":
    unittest.main()
\`\`\`

---

## 호환성 체크리스트

- [ ] Python 3.11.7에서 실행되는가?
- [ ] Git Bash에서 실행되는가?
- [ ] 한글 입출력이 정상인가?
- [ ] 모든 테스트가 통과하는가?

---

**다음**: [10.7-artifact-mapping](10.7-artifact-mapping.md)
