---
name: routing-rules
description: Think Partner Skill에서 자동 라우팅하는 문서 경로 규칙 및 우선순위. Glob 패턴 및 로드 순서 정의.
---

# 문서 라우팅 규칙 (Routing Rules)

Think Partner Skill의 Step 1에서 사용되는 자동 문서 발견 및 로드 규칙입니다.

---

## 🔍 라우팅 규칙 정의

### 로드 순서 및 우선순위

```yaml
Phase 1: 기본 정보 (Priority: 1)
├─ claude.md
│  └─ 프로필 (이름, 상황, 목표)
│  └─ 목표 설정 (4주 목표, 반복 업무 등)
│  └─ 설정 정보 (OS, Python 설치 여부 등)

Phase 2: 자동화 의도 (Priority: 2)
├─ docs/_clarify/*.md (최근 3개 파일, 시간순 역순)
│  ├─ Glob 패턴: docs/_clarify/clarify_*.md
│  └─ 목표: 명확화된 자동화의 의도 파악
│      └─ 각 자동화의 월 시간 절감량
│      └─ 우선순위 (빈도, 소요시간)

Phase 3: 진행 기록 (Priority: 2)
├─ docs/_devlog/*.md (최근 3개 파일, 시간순 역순)
│  ├─ Glob 패턴: docs/_devlog/devlog_*.md
│  └─ 목표: 현재 진행 상황 파악
│      └─ 완료된 자동화 수
│      └─ 예상 진행률

Phase 4: 이전 논의 (Priority: 3)
├─ docs/_think-partner/*.md (최근 2개 파일, 시간순 역순)
│  ├─ Glob 패턴: docs/_think-partner/think_*.md
│  └─ 목표: 누적 성장 추적
│      └─ 이전 방향성 결정
│      └─ 변화 패턴 분석

Phase 5: 성공 패턴 (Priority: 4, Optional)
├─ docs/_patterns/*.md (있으면 자동 로드)
│  ├─ Glob 패턴: docs/_patterns/pattern_*.md
│  └─ 목표: 유사한 성공 패턴 참고
│      └─ 적용 가능한 템플릿
│      └─ 성공 체크리스트
```

---

## 📋 Glob 패턴 상세

### 1. 프로필 정보 (claude.md)

```yaml
경로: claude.md (프로젝트 루트)
파일 존재 필수: Yes
로드 전략: 전체 파일 읽기

추출 정보:
├─ name: 사용자 이름
├─ job: 직업/상황
├─ first-automation: 첫 자동화
├─ 4-week-goal: 4주 목표
├─ repeat-task-1,2,3: 반복 업무
├─ os: 운영체제
├─ python: Python 설치 여부
├─ setup-date: 설정 날짜
└─ completed-automations: 완료된 자동화 수
```

### 2. 자동화 의도 (clarify)

```yaml
경로: docs/_clarify/
패턴: docs/_clarify/clarify_*.md
파일명 형식: clarify_YYYYMMDD_{task-name}.md

로드 전략:
├─ 가장 최신 파일 3개 로드
├─ 시간순 역순 정렬 (최신 우선)
└─ 없으면 스킵 (에러 없음)

추출 정보:
├─ 업무명 (task name)
├─ 빈도 (frequency)
├─ 월 소요시간 (monthly hours)
├─ 연간 절감 시간 (annual savings)
├─ 핵심 불편함 (pain point)
├─ 자동화 기능 (features to automate)
└─ 기대 효과 (expected outcome)

분석 로직:
└─ 월 시간 합계: sum(all monthly hours)
└─ 패턴 감지: 빈도별 그룹화
```

### 3. 진행 기록 (devlog)

```yaml
경로: docs/_devlog/
패턴: docs/_devlog/devlog_*.md
파일명 형식: devlog_YYYYMMDD_{content}.md

로드 전략:
├─ 가장 최신 파일 3개 로드
├─ 시간순 역순 정렬 (최신 우선)
└─ 없으면 스킵

추출 정보:
├─ 진행 단계 (phase/step)
├─ 완료 상태 (status)
├─ 학습 내용 (learnings)
└─ 다음 계획 (next steps)

분석 로직:
└─ 완료된 자동화 수 카운트
└─ 진행률 계산 (3개 목표 기준)
└─ 예상 완료 시점 추정
```

### 4. 이전 논의 (think-partner)

```yaml
경로: docs/_think-partner/
패턴: docs/_think-partner/think_*.md
파일명 형식: think_YYYYMMDD-{title}.md

로드 전략:
├─ 가장 최신 파일 2개 로드
├─ 시간순 역순 정렬 (최신 우선)
└─ 없으면 스킵

추출 정보:
├─ 선택한 방향 (direction: 강점확대/영역확장/역량강화/사업구조변경/기타)
├─ 논의 핵심 (key discussion points)
├─ 결정 사항 (decisions)
└─ 예상 실행 계획 (action plan)

분석 로직:
└─ 이전 결정과 현재 상황 비교
└─ 누적 성장 패턴 분석
└─ 변화 추적
```

### 5. 성공 패턴 (patterns)

```yaml
경로: docs/_patterns/
패턴: docs/_patterns/pattern_*.md
파일명 형식: pattern_YYYYMMDD_{topic}.md

로드 전략:
├─ 모든 패턴 파일 로드 (있으면)
├─ 없으면 스킵 (에러 없음)
└─ 선택적 로드

추출 정보:
├─ 성공 요인 (success factors)
├─ 적용 프로세스 (process steps)
├─ 체크리스트 (checklist)
└─ 적용 팁 (tips)

분석 로직:
└─ 현재 상황과 유사한 패턴 필터링
└─ 적용 가능한 권장사항 도출
```

---

## 🎯 라우팅 알고리즘

### Step 1 실행 흐름

```python
def route_and_load_documents():
    # Phase 1: 프로필 로드
    profile = read("claude.md")

    # Phase 2: clarify 문서 로드
    clarify_files = glob("docs/_clarify/clarify_*.md")
    clarify_recent = sorted(clarify_files, reverse=True)[:3]  # 최신 3개
    clarify_docs = [read(f) for f in clarify_recent]

    # Phase 3: devlog 문서 로드
    devlog_files = glob("docs/_devlog/devlog_*.md")
    devlog_recent = sorted(devlog_files, reverse=True)[:3]   # 최신 3개
    devlog_docs = [read(f) for f in devlog_recent]

    # Phase 4: think-partner 문서 로드
    think_files = glob("docs/_think-partner/think_*.md")
    think_recent = sorted(think_files, reverse=True)[:2]     # 최신 2개
    think_docs = [read(f) for f in think_recent] if think_files else []

    # Phase 5: 패턴 문서 로드 (선택)
    pattern_files = glob("docs/_patterns/pattern_*.md")
    pattern_docs = [read(f) for f in pattern_files] if pattern_files else []

    # 분석
    return analyze_all_documents(
        profile,
        clarify_docs,
        devlog_docs,
        think_docs,
        pattern_docs
    )
```

---

## 📊 분석 결과 형식

### AI가 Step 1에서 제시하는 분석 결과

```
📊 지금까지의 자동화 패턴
├─ [파일명]: [자동화명] → 월 X시간
├─ [파일명]: [자동화명] → 월 Y시간
└─ [파일명]: [자동화명] → 월 Z시간

⏱️ 확보된 자유 (월 기준)
├─ Task 1: 월 X시간
├─ Task 2: 월 Y시간
├─ Task 3: 월 Z시간
└─ 총 월 (X+Y+Z)시간

🎯 설정된 목표 (claude.md)
├─ 목표: [4주 목표]
├─ OS: [운영체제]
├─ Python: [설치 여부]
└─ 설정 날짜: [YYYY-MM-DD]

✨ 이전 Think Partner 논의
├─ [최신 1]: [방향성] - 날짜
└─ [최신 2]: [방향성] - 날짜

💡 참고 가능한 성공 패턴
├─ [pattern 1]: [설명]
└─ [pattern 2]: [설명]
```

---

## ⚠️ 에러 처리 규칙

### 파일 없을 때 처리

```yaml
claude.md:
└─ 필수 파일: Yes
└─ 없으면: 에러 발생 (스킬 중단)

docs/_clarify/:
└─ 필수: No
└─ 없거나 비어있으면: "아직 자동화를 명확화하지 않았습니다" 메시지 후 진행

docs/_devlog/:
└─ 필수: No
└─ 없으면: "아직 진행 기록이 없습니다" 메시지 후 진행

docs/_think-partner/:
└─ 필수: No
└─ 없으면: 이 섹션 스킵

docs/_patterns/:
└─ 필수: No
└─ 없으면: 이 섹션 스킵
```

---

## 🔄 라우팅 우선순위 요약

| 순위 | 경로 | 필수 | 파일 수 | 용도 |
|------|------|------|--------|------|
| 1 | claude.md | ✅ | 1 | 프로필 & 목표 |
| 2 | docs/_clarify/ | ⚠️ | 3 | 자동화 의도 |
| 2 | docs/_devlog/ | ⚠️ | 3 | 진행 상황 |
| 3 | docs/_think-partner/ | ⚠️ | 2 | 누적 논의 |
| 4 | docs/_patterns/ | ❌ | all | 참고용 |

**범례:**
- ✅ = 필수 (없으면 스킬 중단)
- ⚠️ = 선택 (없어도 계속 진행)
- ❌ = 선택적 (있으면 참고)

---

**이 규칙에 따라 Think Partner Skill이 자동으로 맥락을 분석합니다!**
